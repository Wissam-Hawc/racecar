Put your transparent-background robot-car logo here, named exactly:

    logo.png

Recommended: a square-ish PNG with a transparent background, at least
256x256 px. Once the file is here, run `flutter pub get` and rebuild —
the app's connect screen will show it automatically. If the file is
missing, the app falls back to the built-in robot icon.
